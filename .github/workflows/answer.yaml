name: Answers
on:
  workflow_dispatch:


jobs:
  answer:
    runs-on: ubuntu-latest
    steps:
      - env:
          SUPER_SECRET: ${{ secrets.SuperSecret }}
        run: |
          echo "The answer is $SECRET"
          OUTPUT=$SECRET
          echo "${OUTPUT}"
